# Core web framework
fastapi==0.110.0
uvicorn==0.27.1
python-multipart==0.0.9
aiofiles==23.2.1

# Media processing
moviepy==1.0.3
pytesseract==0.3.10
Pillow>=9.0.0
soundfile==0.12.1

# AI/ML libraries
langchain>=0.1.0
langchain-google-genai>=0.0.6
langchain-community>=0.0.35
faiss-cpu>=1.7.0
transformers>=4.36.0
audiocraft>=1.0.0
numpy<2.0

# Utilities
python-dotenv>=1.0.0

# 아래 패키지들은 pip로 직접 설치하기 어려울 수 있으므로 주석 처리합니다
# torch==2.1.0+cu118
# torchvision==0.16.0+cu118
# torchaudio==2.1.0+cu118

# 기본 패키지는 pip로 설치:
# pip install -r requirements.txt

# PyTorch는 CUDA 버전에 따라 설치가 필요할 수 있습니다:
# CUDA 11.8 지원 PyTorch 설치 (conda 사용 권장):
# conda install pytorch=2.1.0 torchvision=0.16.0 torchaudio=2.1.0 pytorch-cuda=11.8 -c pytorch -c nvidia

# 또는 pip로 설치하려면 (환경에 따라 작동하지 않을 수 있음):
# pip install torch==2.1.0+cu118 torchvision==0.16.0+cu118 torchaudio==2.1.0+cu118 --index-url https://download.pytorch.org/whl/cu118

# pytesseract를 위한 Tesseract OCR 엔진 설치:
# Windows: https://github.com/UB-Mannheim/tesseract/wiki 에서 설치 프로그램 다운로드
# macOS: brew install tesseract
# Ubuntu/Debian: sudo apt-get install tesseract-ocr
# 또는 conda: conda install -c conda-forge tesseract